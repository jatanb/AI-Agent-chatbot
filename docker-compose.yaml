version: "3.9"

services:
  app:
    build: .
    container_name: scheme-scout
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped